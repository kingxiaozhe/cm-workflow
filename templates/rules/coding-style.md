---
description: {一句话：本项目的编码风格约定}
---

<!-- 模板骨架 · 由 /cm-init 或 bootstrap 以此为骨架生成，生成时必须遵守四原则：
     ① 可执行——具体指令，禁止"使用恰当的 XX"类模糊话
     ② 关键规则带 Bad/Good 对比示例
     ③ 量化——用数字不用形容词
     ④ 现代实践——锚定项目当前版本的惯用法
     所有 {占位符} 结合项目实际填充；不适用章节删除；禁止保留空洞通用话术 -->

# 编码风格

## 量化标准（默认值，按团队口味调整）

| 项 | 上限 |
| ---- | ---- |
| 函数长度 | {30} 行 |
| 文件长度 | {300} 行 |
| 嵌套深度 | {3} 层 |
| 函数参数 | {4} 个 |

## 命名

- 文件命名：{从项目现有文件推断，如 kebab-case / PascalCase 组件}
- 变量/函数：{camelCase 等，附一个项目内真实示例}
- 常量与枚举：{约定}

## Import 与文件组织

- Import 顺序：{如 内置 → 第三方 → 别名 → 相对路径，从 lint 配置推断}
- 路径别名：{tsconfig/构建配置中的实际别名清单}

## 注释

- 只解释"为什么"，不复述"做了什么"
- {公共 API 的文档注释要求，如 JSDoc / docstring}

## Bad / Good 示例

```{lang}
// Bad: {从项目痛点提炼}
// Good: {对应正确写法}
```
